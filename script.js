document.addEventListener("DOMContentLoaded", (function () { let e = document.getElementById("main-content"), t = document.querySelectorAll('[id^="project"]'), l = document.querySelectorAll(".view-project-btn"), c = document.querySelectorAll(".back-to-main"), o = document.getElementById("mobile-nav"), i = l => { if (window.scrollTo(0, 0), e.classList.remove("active"), t.forEach((e => e.classList.remove("active"))), "main-content" !== l && l) { let t = document.getElementById(l); t ? (t.classList.add("active"), o.classList.add("hidden")) : (e.classList.add("active"), o.classList.remove("hidden")) } else e.classList.add("active"), o.classList.remove("hidden") }; l.forEach((e => { e.addEventListener("click", (() => { let t = e.dataset.page; i(t) })) })), c.forEach((e => { e.addEventListener("click", (t => { t.preventDefault(); let l = e.getAttribute("href"); i("main-content"), l && l.startsWith("#") && setTimeout((() => { let e = document.querySelector(l); e && e.scrollIntoView({ behavior: "smooth" }) }), 0) })) })); let n = document.querySelectorAll("#main-content section"), a = document.querySelectorAll(".nav-link"), r = document.querySelectorAll(".nav-mobile-link"), s = new IntersectionObserver((e => { e.forEach((e => { if (e.isIntersecting) { let t = e.target.id; a.forEach((e => { e.classList.toggle("active-nav", e.getAttribute("href").substring(1) === t) })), r.forEach((e => { e.classList.toggle("active-icon", e.getAttribute("href").substring(1) === t) })) } })) }), { root: null, rootMargin: "0px", threshold: .4 }); n.forEach((e => { e.id && s.observe(e) })); let d = document.querySelectorAll(".filter-btn"), m = document.querySelectorAll(".portfolio-item"); d.forEach((e => { e.addEventListener("click", (() => { d.forEach((e => e.classList.remove("active"))), e.classList.add("active"); let t = e.dataset.filter; m.forEach((e => { e.style.display = "none", ("all" === t || e.dataset.category === t) && (e.style.display = "block") })) })) })) }));
